name: particle_flow
description: "High-performance configurable particle system for Flutter (canvas + widgets, with overlay child support)."
version: 0.0.1
homepage: https://github.com/your-user/particle_flow
repository: https://github.com/your-user/particle_flow
issue_tracker: https://github.com/your-user/particle_flow/issues

environment:
  sdk: ">=3.10.1 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
